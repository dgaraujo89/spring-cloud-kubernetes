
server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: ${GIT_REPO_URL}
          username: ${GIT_REPO_USER}
          password: ${GIT_REPO_PASSWORD}
            